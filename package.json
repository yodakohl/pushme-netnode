{
  "name": "pushme-netnode",
  "version": "0.1.0",
  "description": "Minimal network agent that measures connectivity and publishes events to PushMe.",
  "type": "module",
  "bin": {
    "pushme-netnode": "./src/index.mjs"
  },
  "scripts": {
    "setup": "node ./scripts/setup.mjs",
    "preview": "node ./scripts/preview-coverage.mjs",
    "start": "node ./src/index.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
